"""vaultcheck — Password strength analyzer + HIBP breach checker.

Computes password entropy, runs lightweight weakness heuristics, and
cross-references passwords against the HaveIBeenPwned (HIBP) k-anonymity
API — the password itself is never transmitted.

Part of the Ghostline cybersecurity suite.
Ghostline: Invisible. Inevitable.
"""

__version__ = "0.1.0"
__author__ = "Ghostline / Mellowambience"
